/**
 * Primitivos (imutáveis): String, Number, Boolean, undefined, null (bigint e symbol)
 *  - valores copiados: quando se altera esses valores, cria-se uma cópia
 *
 *  Referência (mutáveis): array, object e function
 *  - valores são passados por referência
 */